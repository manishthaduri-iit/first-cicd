apiVersion: apps/v1
kind: Deployment
metadata:
  name: vibestream-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vibestream
  template:
    metadata:
      labels:
        app: vibestream
    spec:
      containers:
      - name: vibestream
        image: manishthaduri26/devops-demo:latest
        imagePullPolicy: IfNotPresent  # Use local image if available
        ports:
        - containerPort: 5000
        env:
        - name: FLASK_ENV
          value: "production"
